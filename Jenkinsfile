pipeline {
    agent any
       stages {
        stage("Application Sync") {
            steps {
                 sh """
                 aws --version
         
                }
            }
        }
    }
